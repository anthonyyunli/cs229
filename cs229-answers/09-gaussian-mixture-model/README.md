# Gaussian Mixture Models

Expectation-maximization for a four-component Gaussian mixture, with and without labeled examples. The script runs three trials of each and saves the cluster plots to `output/`.

Run:

```bash
python 09-gaussian-mixture-model/gmm.py
```
