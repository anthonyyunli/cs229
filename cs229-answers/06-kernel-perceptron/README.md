# Kernel Perceptron

Perceptron classification with dot-product and RBF kernels. The example plots both decision boundaries and saves test predictions to `output/`. The prediction function uses the training arrays loaded by the script.

Run:

```bash
python 06-kernel-perceptron/kernel_perceptron.py
```
