# Poisson Regression

Poisson regression fitted by gradient ascent. The example uses a learning rate of `2e-7` and plots predicted counts against the labels.

Run:

```bash
python 04-poisson-regression/poisson_regression.py
```
