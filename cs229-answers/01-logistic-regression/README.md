# Logistic Regression

Newton's method for binary classification. Each call to `fit` takes one Newton step; the example runs 100 steps and plots the decision boundary. Input features include an intercept column.

Run:

```bash
python 01-logistic-regression/logistic_regression.py
```
