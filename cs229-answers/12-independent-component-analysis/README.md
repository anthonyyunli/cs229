# Independent Component Analysis

Separating five mixed audio tracks with ICA, using a Laplace source model and an annealed learning rate. The script writes the separated WAV files and unmixing matrix to `output/`.

Run:

```bash
python 12-independent-component-analysis/ica.py
```
