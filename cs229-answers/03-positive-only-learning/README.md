# Positive-Only Learning

An experiment with incomplete positive labels. It compares logistic regression on true labels and observed labels, then estimates a correction from the validation set.

Run:

```bash
python 03-positive-only-learning/positive_only.py
```
