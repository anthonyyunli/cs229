# CartPole

A CartPole controller that learns transition probabilities and rewards from experience, then updates its policy through value iteration. The script saves a learning curve to `output/`.

`env.py` is the supplied CS229 simulator.

Run:

```bash
python 13-reinforcement-learning/cartpole.py
```
