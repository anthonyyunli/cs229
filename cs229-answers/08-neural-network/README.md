# Step-Activation Neural Network

Weights for the step-activation network in PS3, problem 1. The script prints the weights and saves them to `output/step_weights.json`.

Run:

```bash
python 08-neural-network/neural_network.py
```
