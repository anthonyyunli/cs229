# Naive Bayes

SMS spam classification using word counts and multinomial Naive Bayes. The script builds a vocabulary, fits the model, prints test accuracy and indicative words, and compares against an SVM with a radius chosen on the validation set.

`svm.py` is the supplied CS229 SVM implementation.

Run:

```bash
python 07-naive-bayes/naive_bayes.py
```
