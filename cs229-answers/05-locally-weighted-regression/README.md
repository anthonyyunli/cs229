# Locally Weighted Linear Regression

Weighted least squares fitted around each query point. The script compares bandwidths on the validation set and uses `tau=0.05` for the final test predictions.

Run:

```bash
python 05-locally-weighted-regression/locally_weighted_regression.py
```
